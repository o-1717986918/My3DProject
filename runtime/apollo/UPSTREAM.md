# Upstream provenance

This runtime was imported from the public ApolloCodebase repository rather
than copied from the former local submodule working tree.

- Upstream: <https://github.com/XiangruiJiang/ApolloCodebase>
- Imported branch: `main`
- Imported commit: `71018c968969d6e55130b0e1987cd5b4f5c3b4df`
- Import date: 2026-08-31
- Licence: GPL-3.0-or-later

The import was checked with `git ls-remote`, a fresh recursive network clone,
and the GitHub branches/releases API. At the import date, upstream exposed one
branch (`main`), no tags, no releases, no Git LFS objects, and no submodules.
The two ONNX assets were verified byte-for-byte after import.

My3DProject changes remain marked with the same GPL SPDX identifier. Update
this file whenever a later upstream revision is merged.
