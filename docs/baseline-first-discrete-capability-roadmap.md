# 原生 Apollo 基线优先开发路线

状态：执行中  
日期：2026-09-11

## 决策

从原生 Apollo 提交 `71018c968969d6e55130b0e1987cd5b4f5c3b4df` 重新开始开发。当前 `runtime/apollo` 不删除，作为动作、模型和策略候选库；但它的全部改动不再默认进入正式队伍。

新路线只遵守三条：

1. 一次只加入一个能清楚解释的能力或策略。
2. 先确认能运行且不违反实际比赛规则，再与原版做换边对照。
3. 能稳定改善对应问题的就保留；无改善或副作用更大的立即回退，不为已有投入辩护。

不先建设通用策略平台、大型能力注册系统或复杂综合评分。测试脚本和日志只做到足够回答“这项改动是否值得保留”。

## 对需求的关键修正

- “离散化”只用于几个会改变行为优先级的比赛状态，不把连续运动控制离散化。
- 比分是最终指标，但单场比分随机性很大；同时查看近球停滞、有效触球、到球时间、跌倒和犯规，帮助判断原因。
- 规则书与服务器实现不完全一致。本机 RCSSServerMJ `0.2.1` 是当前运行真值，正式规则书是兼容上限。
- 动作层优先。阵型、支援和盯人不得让合法追球者在球边停住。
- 程序化踢球和学习踢球采用各自的释放条件，不能让静态动作门槛提前拦截动态过渡模型。
- 2D 策略只作低优先级参考。只借鉴唯一追球者、到达时间、接球超时、支援候选点等结构，不复用 2D 运动学和数值。

## 当前规则边界

本机固定环境：RCSSServerMJ `0.2.1`、`ssim26`、7v7、55 m × 36 m、每半场 300 秒。

直接影响开发的规则：

- 开球不能直接得分，执行者在其他球员触球前不能再次触球。
- 角球、界外球和任意球时，对手须离球 5.5 m；球门球时对手须离开球门区。
- 本方球门区最多两名球员。
- 本机定位球超时为 15 秒，动作应明显早于此时间完成。
- 非门将倒地 15 秒或未站立 30 秒会被重置；门将对应为 30 秒和 60 秒。
- 队员间只能通过服务器 `say` 通信，官方规则限制每周期 10 Bytes。

官方依据：[RoboCup 2026 规则书](https://ssim.robocup.org/wp-content/uploads/2026/06/Rules_RoboCupSim3D2026.pdf)、[RCSSServerMJ](https://gitlab.com/robocup-sim/rcssservermj)。上游主干正进行裁判重构，因此实验不随意升级服务器。

## 只保留必要的离散状态

决策层先判断：

1. `Fallen`：起身，任何策略不得覆盖。
2. `OurRestart`：指定一名执行者，其余球员合法让位。
3. `TheirRestart`：合法避让并保持防守位置。
4. `PlayOnNearBall`：追球、调整或触球；禁止阵型覆盖导致停滞。
5. `PlayOnAwayBall`：才允许支援、盯人或阵型。
6. `GameOver/Unknown`：安全保持。

连续位置、速度、角度和球轨迹继续交给动作代码与模型处理。

## 候选资产

| 候选 | 处理方式 |
|---|---|
| 原生 Walk/GetUp、追球和推球 | 新开发线的默认基线 |
| RapidTurn | 单独接入和对照 |
| FastWalk | 单独接入和对照；允许跌倒，但比较有效到球时间与恢复成本 |
| 程序化参数踢球 | 作为可解释回退，单独验证传球/射门距离与方向 |
| kick-transition ONNX | 使用动态入口条件，单独验证 |
| 其他踢球/带球/起身 ONNX | 有有效本地结果才接入 |
| 支援、盯人、传球通信 | 动作层稳定后逐项接入，不捆绑开启 |

[ICRA 2026 striker 官方实现](https://github.com/Daffan/humanoid-soccer)证明“动态追球直接衔接定向踢球”的训练路线值得参考，但不证明它能直接迁移，也不为复杂全局策略背书。

## 简化验证办法

每项改动只做三步：

1. 单元或固定场景测试：确认触发、完成和失败回退。
2. 两边互换的短比赛：先各两场；结果接近时再增加样本，不预先强制大量比赛。
3. 看比分和该能力的直接指标。例如转身看转向耗时与跌倒，踢球看触发率、球速和方向误差，策略看近球停滞和有效推进。

出现规则错误、崩溃、明显增加停滞或对应指标没有改善，就关闭或回退。只有结果能复现时才在文档中写“保留”。

## 执行顺序

### 0. 建立新基线

- 将原生 Apollo 复制为独立开发目录。
- 构建并确认 7 名球员可运行。
- 与原生版本核对源码和初始行为。

### 1. 先修比赛中的直接阻塞

- 门将能在球门球中及时开球。
- 球员在球边不能因阵型或空决策长时间停住。
- 跌倒时可靠切换到起身。
- 规则状态不会泄漏错误动作。

### 2. 提升移动

依次测试 RapidTurn、FastWalk、先转身再前进。每次只启用一个。之后再训练稳定转身、快速前进和横向运动模型，训练输出只放 `/home/win98/rl_runs`。

### 3. 提升触球

- 先放宽和修正近球触发，保证原生接触动作能释放。
- 程序化参数踢球作为回退。
- 动态 kick-transition 单独接入，不受静态轨迹腿速门槛限制。
- 再训练长传、射门和带球；分别验收，不混成一个 Kick 能力。

### 4. 最小球队策略

在动作可用后依次测试：唯一追球者、接收者通信、单一支援、单一盯人、传球选择。阵型始终低于近球动作优先级。2D 代码只在具体问题需要时检索。

## 第一批立即执行

1. 导入并构建原生开发线。
2. 做一个最小的基线启动/对照脚本。
3. 先审查并修复原生门将球门球与近球停滞，作为第一项候选改动。

最终目标不是功能最多，而是用最少且确实有效的改动稳定击败原版。

## 执行记录

- `16ff950`：原生 72 个文件完整导入；新开发线与冻结原版完成左右换边启动检查，两次均为 14/14 客户端存活且无客户端错误。
- **已否决并撤销：门将球门球推球候选。** 通过服务器真实出界触发球门球，两次对照中“先对齐再推球”均约 6.2 s 才恢复 PlayOn，原版直接走向球点仅约 2.5–2.6 s。候选更慢，代码恢复原版；启动脚本保留该强制场景用于后续回归。
- 最小遥测首轮：3 个 `PlayOn + AP + 距球不超过 1 m` 样本仍输出 0.87–1.31 m/s 的行走目标，没有发现策略置零；另一次运行出现 2 个采样周期的双 AP。当前证据不足以重写阵型或角色系统。
- RapidTurn 候选：只增加一个显式开关，复用已训练模型；大角度移动命令先原地转身，球周 0.70 m、非 PlayOn 或姿态不稳时仍走原生路径。左右换边短赛均为 14/14 客户端存活，共采样到 15 次左转、14 次镜像右转，球周误接管和模型错误均为 0。
- 上述结果只证明 RapidTurn 能正常挂载，尚未证明比分或到球时间改善，因此仍是候选，不标记为正式保留。下一次只比较它的转向/到球收益；若无收益就撤销。
- FastWalk 候选：补做换边短赛后，左侧 236 个接管采样无起身，右侧 196 个接管采样却有两名球员跌倒并分别产生 150/65 个起身采样。进一步发现相对 Walk 命令绕过了原意中的“目标至少 2 m”检查，模型甚至在距球 0.28–0.50 m 时接管；收缩为“仅 AP 且距球至少 2 m”后左右两场均为 0 次接管，没有比赛收益。该集成收缩已撤销，模型继续默认关闭；不再为旧模型叠加门控，只有重新训练得到稳定且命令覆盖匹配的新模型后才复评。
- **正式保留：AP 近球追踪。** 原版 AP 仍执行全场扫视，基线的 67 个 PlayOn 近球样本中球可见率为 0。改为 AP 直接看当前最佳球位后，左右换边分别达到 20/53 和 19/19，观测到约 1.78 m/s 与 2.05 m/s 的真实触球球速；两场均为 14/14 客户端存活且 0 起身。其他场上角色仍按原版扫描。
- **正式保留：防守开球球门区合法性。** 原版将 6、7 号放在 `x=-23.5,y=±3`，恰好位于球门区边界内，右侧比赛会连同门将形成三人进入并固定处罚 6 号。两名深卫前移到 `x=-23.0` 后，规则断言通过，连续两场右侧开球均为 14/14 存活且不再出现 `Illegal defense`。
- 对照启动器改为等 14 名球员全部激活后再开球，避免后启动队尚未完成 Beam 就进入比赛。换边对照固定由左队开球；本机服务器在 `BeforeKickOff` 已按左队开球安排 Beam，赛前再改为右队开球不会重新 Beam，不能作为有效对照。
- **已否决并撤销：将 AP 推球目标从球后 1.0 m 增至 1.5 m。** 两次固定开球把首次球速从约 1.83 m/s 提到约 1.96 m/s，但右侧自然比赛中 1.5 m 候选的清晰触球峰值约 2.75 m/s，原版 1.0 m 同长度对照达到约 4.81 m/s，且候选产生更多球周滞留样本；两者均无起身。继续保留原值，长传/射门提升转入独立动作与衔接训练。
- **不挂载：宽松追球到踢球释放教师。** 复用同一追球 checkpoint 和 2 m 踢球先验，仅将稳定条件从 `0.35 m/s + 5 帧` 放宽为 `0.50 m/s + 2 帧`。训练内最终评估从严格版 12/64、0 跌倒降至 8/64、2 跌倒；独立同种子 128 回放则为 18/128、1 跌倒，对照严格版 15/128、1 跌倒。方向不一致，不能证明提升。产物保留在 `/home/win98/rl_runs/striker/contact-release-2m-relaxed-s20261228-v1` 供后续衔接研究，当前比赛队伍不启用，也不追加调参轮次。
- **已否决并撤销：把球后准备点容差从 0.25 m 放宽到 0.35 m。** 左侧短赛中 AP 近球滞留样本从旧基线 21 增到 570，较高球速样本占比由约 38%降到约 10%；右侧由 270 增到 1522，占比由约 4.1%降到约 1.4%。过早进入推球阶段使未对正的球员更久困在球周，代码恢复原值。现有日志也确认近球低速时决策仍持续输出 Walk 目标，首要问题是运动执行与触球衔接，不是阵型返回空命令。
- **不挂载：直线航向锁定 FastWalk 续训。** 在相同种子、64 条独立 10 s 回放中，旧模型与候选均 64/64 直立；中位前向速度由 1.437 m/s 增至 1.465 m/s，中位横向漂移只由 4.93 m 降至 4.71 m（约 4.5%），航向误差反而由 0.184 增至 0.195 rad。候选未通过直接指标，不进入服务器，临时训练配置已删除；产物保留在 `/home/win98/rl_runs/stable-motion/fast-forward-yaw-lock-s20261310-v1`。下一步只审查训练目标是否缺少累计航向或横向位移信号，不再盲目加大瞬时速度惩罚。
- 审查确认 `DirectionalRun` 只奖励瞬时本体速度和角速度，没有累计航向或横向位移反馈；策略输入也没有绝对航向。另修正 checkpoint 评估器的口径：直线评估不再随机初始朝向后直接用世界 Y 计漂移。上述 4.93/4.71 m 来自已按初始朝向投影的 CPU/ONNX 评估，因此否决结论不变；在设计出不改变部署契约的目标方向反馈前暂停 FastWalk 调参。
- RapidTurn 开/关各做了一轮左右换边的 15 s 无渲染短赛。开启组分别采到 5 次和 3 次接管，四场均为 14/14 客户端存活且 0 起身，但没有证据表明到球或推进更快；因此继续默认关闭，也不为它增建复杂门控或遥测。后续若固定转向场景不能直接证明收益，就从新开发线移除该可选入口。
- 新增一个可选的近球固定场景，只移动球员和球，不改变决策或动作。球员位于球后、左右偏置 `0.15/0.30 m`、球正侧方以及球前方时都能触球，所有运行均 0 起身；峰值球速约 `2.25--3.49 m/s`。正后方首次明显触球约需 1.14 s，最困难的球前方场景约需 3.84 s。该组场景没有复现永久停滞，因此不继续放宽触发条件；场景保留用于未来动作改动的同条件 A/B。
- 当前最小保留版完成两场 600 s 换边比赛：开发版在左侧 `2:0`，在右侧 `3:0`，总比分 `5:0`。两场开发版均为 0 次非法防守，FastWalk/RapidTurn 均未启用；首场有同一球员的一次短暂起身恢复（2 个周期采样），换边完整场为 0。原版位于右侧时因原始球门区边界站位产生 71 次处罚，位于左侧的完整场只有 2 次比赛中后段处罚。换边结果消除了首场大量处罚作为唯一胜因的解释，足以在当前固定服务器、规则、开球方式和版本条件下判定最小保留版优于冻结原版；它不外推为对其他队伍或不同规则环境的结论。此前 180 s 换边短赛为 `0:0`，作为单场短样本随机性证据保留。
- **移植前否决：旧程序化踢球整套入口。** 旧资产已测的推球、射门和解围峰值约为 `0.87/2.75/3.48 m/s`，而新基线的固定近球走球已达到 `2.25--3.49 m/s`；旧 runner 在自然比赛中还要求更窄的静态球位和身体状态。现有证据不能证明移植会提升比赛动作，因此不把旧 KickCommand、profile、runner 和策略选择器带入新开发线。其传球方向性资产仍保留在旧候选库，待有独立成功率证据再评估。
- 冻结原版与新开发线逐文件核对后，默认比赛行为差异仍只有 AP 近球看球与防守开球站位；其他差异是可选 FastWalk/RapidTurn 入口、动态 ONNX 批尺寸兼容、状态日志和测试，两个学习动作开关默认关闭。旧版支援、盯人、传球与综合战术没有隐式进入新开发线。
